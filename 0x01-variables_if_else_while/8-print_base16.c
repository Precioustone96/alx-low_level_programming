# include <stdio.h>
/**
 * main - point of Entry for my code
 * Return: output a 0 when successful
 */

int main(void)
{
	int z;

for (z = 48; z < 58; z++)
{
putchar(z);
}
for (z = 'a'; z <= 'f'; z++)
{
putchar(z);
}
putchar('\n');
return (0);
}
